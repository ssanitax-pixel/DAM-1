En este ejercicio, vamos a crear un programa en Python que saluda a los amigos antes de mostrar "Hola mundo". Usaremos funciones 'def' para organizar el código en bloques reutilizables y 'print' para mostrar mensajes en pantalla. Esto nos ayudará a entender cómo estructurar un programa y controlar el flujo de ejecución, asegurando que el saludo se muestre antes del mensaje principal.

Definiremos la función.

```
def saludar_amigos():
```

Impriminos el saludo, importante usar el tabulador para sangrar correctamente.
```
	print("¡Hola, amigos!")
```

Luego mostramos el mensaje de "Hola mundo".

```
print("Hola mundo desde Python")
```

Aquí está el programa completo:

```
def saludar_amigos():
    print("¡Hola, amigos!")

def __main__():
    saluda()
    
print("Hola mundo desde Python")
```

Este ejercicio nos permite entender la importancia de organizar el código en funciones para hacerlo más modular y fácil de mantener. Al crear la función saludar_amigos(), vemos cómo dividir tareas específicas dentro del programa y cómo controlar el orden en que se ejecutan. En proyectos futuros, esta técnica de estructurar código con funciones será clave para mantener programas ordenados y fáciles de entender.
