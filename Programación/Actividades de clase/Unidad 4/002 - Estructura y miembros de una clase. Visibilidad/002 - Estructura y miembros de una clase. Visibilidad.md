Declaramos una clase llamada Cliente, con los atributos de email, nombre y dirección. Iniciamos estos atributos con valores nulos.

```
class Cliente():
    def __init__(self):
        self.email = email
        self.nombre = nombre
        self.direccion = direccion
```

Implementamos un método para crear un cliente, donde el usuario pueda introducir los datos del cliente.

```
email = input("Dime el email del cliente: ")
```


